#This is a simple project to learn mocha, chai and webdriver testing frameworks

##Installation

Install `mocha` globally:
```javascript
$ npm install mocha -g
```

Install local dependencies:
```javascript
$ npm install
```

##Running tests

```javascript
$ npm test
```